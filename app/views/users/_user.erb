<li>
    <%= gravatar_for user, size: 50 %>
    <%= link_to user.name, user %>
    <% if can_destroy_user? user %>
    | <%= link_to t(".delete"), user, data: { turbo_method: :delete, turbo_confirm: t(".are_you_sure?") } %>
    <% end %>
</li>
